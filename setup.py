from setuptools import setup,find_packages

setup(
    name="src",
    version="0.0.1",
    description="its a Google Analytics package",
    author = "priya",
    packages = find_packages(),
    license="MIT"
)